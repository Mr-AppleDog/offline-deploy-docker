# Kunlun 离线 Docker 部署总则

本文是 Kunlun 离线部署的权威入口。目标服务器统一使用 `/opt/Kunlun`，中间件固定部署，应用按版本更新。

## 1. 文档职责

| 文档 | 作用 |
|---|---|
| [目录规划](docs/目录规划.md) | 定义 `/opt/Kunlun` 唯一合法目录、配置来源和权限 |
| [V1 手工版](docs/V1-手工版.md) | 在全新服务器完整部署 `1.1.1` |
| [1.1.1 实测记录](docs/1.1.1-手工部署实测记录.md) | 保存简化结构实际命令阶段、镜像 ID、健康和容量结果 |
| [应用升级手工版](docs/应用升级手工版.md) | 部署 `1.1.2` 及以后应用和数据库变更 |
| [V1 实测记录](docs/V1-实测记录.md) | 记录 `1.1.0` 旧结构实测事实，不作为新结构操作步骤 |
| [V2 脚本版](docs/V2-脚本版.md) | 把已经验证的手工步骤自动化 |

## 2. 固定决策

- Linux 根目录固定为 `/opt/Kunlun`，大小写不得变化。
- 不使用 `packages`、`releases`、`current`、`state` 和运行时 `kunlun.env`。
- 四个中间件固定为 MySQL、Redis、RabbitMQ 和 MinIO，各自拥有独立镜像、数据和备份目录。
- 中间件由一份 `compose.middleware.yml` 管理，后续应用升级默认不修改它。
- backend/frontend 由一份 `compose.app.yml` 管理，应用镜像 tar 按版本保存。
- 两套 Compose 通过外部网络 `kunlun-net` 通信。
- 所有镜像固定标签，禁止 `latest`；离线运行禁止自动拉取。
- 数据库迁移跟随应用版本，不通过升级 MySQL 镜像代替表结构迁移。
- 禁止 `docker compose down -v`，禁止删除中间件数据目录处理启动问题。

## 3. 生效配置

| 文件 | 是否生效 | 内容 |
|---|---|---|
| `/opt/Kunlun/middleware/compose.middleware.yml` | 是 | 中间件镜像、各组件独立账号密码、端口、数据挂载 |
| `/opt/Kunlun/application/compose.app.yml` | 是 | 应用镜像、后端连接地址和相同凭据副本 |
| `/opt/Kunlun/middleware/mysql/conf.d/*` | 可选 | MySQL 附加配置 |
| `/opt/Kunlun/database/init/*` | 仅首次 | 全新 MySQL 数据目录的初始化 SQL |
| `/opt/Kunlun/database/migrations/<版本>/*` | 升级时 | 分版本数据库迁移 SQL |
| 镜像 tar、SHA256、manifest | 否 | 安装和审计材料，不是运行配置 |

`compose.middleware.yml` 和 `compose.app.yml` 必须是 `root:root 0600`。Web 平台按站点生成 MySQL、Redis、RabbitMQ、MinIO 的独立密码。因为两份 YAML 独立解析，backend 所需业务凭据必须保存相同副本，并在构建和部署时校验一致性。

## 4. 两种离线包

### 4.1 完整初始化包

`kunlun-bootstrap-1.1.1-linux-amd64.tar.gz` 用于全新服务器，包含：

- Docker Engine 和 Compose 离线介质；
- 四个中间件镜像 tar；
- backend/frontend `1.1.1` 镜像 tar；
- 两份 Compose；
- 初始化 SQL、脚本、文档和校验文件。

### 4.2 应用更新包

`kunlun-app-update-1.1.2-linux-amd64.tar.gz` 用于已有 Kunlun 环境，只包含：

- backend/frontend 新版本镜像 tar；
- 新的 `compose.app.yml`；
- 可选数据库迁移 SQL；
- manifest 和 SHA256。

更新包不得重复携带或覆盖 Docker、中间件镜像、中间件 Compose 和中间件数据。

## 5. 首次部署顺序

```text
校验完整初始化包
  → 建立简化目录
  → 安装 Docker/Compose
  → 确认 Docker data-root
  → 导入四个中间件镜像
  → 导入 backend/frontend 镜像
  → 安装两份敏感 Compose
  → 初始化数据目录权限
  → 创建 kunlun-net
  → 启动并验收中间件
  → 启动并验收应用
  → 执行首次备份与恢复演练
```

逐条命令见 [V1 手工版](docs/V1-手工版.md)。

## 6. 应用升级顺序

```text
校验应用更新包
  → 校验当前中间件健康
  → 备份数据库和当前 app Compose
  → 导入新应用镜像
  → 执行数据库迁移
  → 替换 application/compose.app.yml
  → 只重建 backend/frontend
  → 业务验收
```

逐条命令和失败处理见 [应用升级手工版](docs/应用升级手工版.md)。

## 7. 成功标准

首次部署只有同时满足以下条件才算完成：

1. 外层和包内 SHA256 全部通过。
2. 六个镜像标签、ID 和平台与清单一致。
3. Docker Root Dir 为 `/opt/Kunlun/docker/data`。
4. 四个中间件均为 `healthy`。
5. backend 和 frontend 均为 `healthy`。
6. `/api/health/all` 顶层返回 `"ok":true`。
7. MySQL 业务连接使用专用业务账号而不是 root。
8. 中间件数据实际写入 `/opt/Kunlun/middleware/<组件>/data`。
9. 完成一次备份和只恢复到演练目录的恢复测试。

## 8. 实施状态

- 已完成：`1.1.0` 旧目录核心手工实测。
- 已完成：`1.1.1` 简化目录、Compose、脚本和 bootstrap 包。
- 已完成：在全新虚拟机完成 `1.1.1` 核心手工部署。
- 待完成：首次备份和恢复演练。
- 待完成：将新增 `install-bootstrap.sh` 打入独立 `r2` 包并在另一台全新虚拟机复验。
- 最后实施：应用更新包及 V2 自动化升级验收。
