# 离线 Docker 部署手册

> 适用：目标机 **x86_64 · 完全离线 · 操作系统不确定（无任何环境）**。
> 方案：目标机用 **Docker 官方静态二进制包**装 Docker（免 rpm/deb 的依赖地狱），
> 应用与中间件在**构建机（有网）**打好镜像整体 `docker save`，拷到目标机 `docker load + compose up`。
>
> ⚠️ 本手册是**唯一权威入口**，覆盖「构建 → 打包 → 装 Docker → 部署 → 验收」全链路。
> 历史分散的部署文档（根目录 `01-离线包制作.md` / `02-服务器安装与首次部署.md`、`doc/`、`backend/doc/`）
> 已全部清理、合并进本手册，避免多份不一致。

---

## 0. 总体思路（一张图）

```
┌─────────────── 构建机（有网：docker + maven + jdk17 + node）───────────────┐
│  ① mvn package 打后端 jar         ② docker build 前后端镜像                  │
│  ③ docker pull 4 个中间件镜像      ④ docker save 全部 6 个镜像 → all-images.tar│
│  ⑤ 下载 docker 静态二进制 + compose 二进制                                   │
│  输出：offline-kit/（拷进 U 盘）                                             │
└───────────────────────────────────────────────────────────────────────────┘
                                    │  U 盘 / 摆渡机
┌─────────────── 目标机（完全离线，无任何环境，x86_64）───────────────────────┐
│  ① 解压 docker 静态包 → /usr/bin，systemd 托管 dockerd                       │
│  ② 装 compose 插件（让 `docker compose` 可用）                               │
│  ③ docker load -i all-images.tar                                            │
│  ④ 放部署目录 + 初始化目录权限 + 改 .env                                      │
│  ⑤ docker compose up -d → 6 个容器全 Up → 验收                               │
└───────────────────────────────────────────────────────────────────────────┘
```

**核心原则一句话**：把「需要联网的事」全压在构建机做，目标机只做「拷贝 + load + up」，全程不联网、不装 Maven/npm。

---

## 1. 目标机最小要求

| 项 | 要求 | 自查命令 |
|---|---|---|
| 架构 | **x86_64** | `uname -m` → 必须输出 `x86_64` |
| 内核 | 建议 4.x+（支持 overlayfs / cgroup） | `uname -r` |
| 内存 | ≥ 4 GB（给 MySQL + 4 中间件留余量） | `free -h` |
| 磁盘 | ≥ 20 GB 可用（镜像 tar + 中间件数据） | `df -h` |
| 系统 | 任意主流 Linux（CentOS/麒麟/Ubuntu 均可，方案不挑发行版） | `cat /etc/os-release` |

> 若架构输出 `aarch64`，本手册的镜像与安装包都要换成 arm64 版本（见文末 §8）。

---

## 2. 构建机操作（有网，一次性产出离线包）

构建机 = 任意有网的 Linux。推荐直接用你那台 Ubuntu dev 服务器（192.168.149.128，已装 docker）。
若用 Windows 本机构建，注意：**npm 在中文路径下会失败**，前端要拷到纯 ASCII 路径；后端 Maven 同理（见 README 注意事项）。

### 2.1 准备构建机环境

```bash
java -version        # 需 17+
mvn -v               # Maven 3.6+
docker --version     # 20.10+ 即可，单架构构建不需要 buildx
```

缺 Maven/JDK17 就装（有网机器直接 `apt install maven openjdk-17-jdk` 之类）。

### 2.2 打后端 jar

```bash
cd /opt/build/offline-demo/backend
mvn -DskipTests package               # 首次会联网拉依赖，耐心等
# 产物：target/offline-demo-0.0.1-SNAPSHOT.jar（jar 名 = artifactId-version，见 pom.xml）
ls target/offline-demo-0.0.1-SNAPSHOT.jar
```

### 2.3 构建后端镜像

```bash
cd /opt/build/offline-demo/backend
docker build -t offline-demo-backend:1.0 .
# Dockerfile 只是 COPY jar 到 eclipse-temurin:17-jre，构建本身不再联网
```

### 2.4 构建前端镜像

```bash
cd /opt/build/offline-demo/frontend
docker build -t offline-demo-frontend:1.0 .
# 阶段1 node:20-alpine 里 npm install 需要联网（只在构建机做）
```

> ✅ 已为 frontend 补了 `.dockerignore`（忽略 `node_modules`/`dist`），否则 `COPY . .` 会把本机几万文件、甚至 Windows 原生依赖打进构建上下文，既慢又可能带错架构。

### 2.5 拉取中间件镜像（锁版本，**不要用 latest**）

```bash
docker pull mysql:8.4
docker pull redis:8.2
docker pull rabbitmq:4.3-management
docker pull minio/minio                        # 先拉 latest，只为拿到真实 RELEASE 标签
docker image ls minio/minio                    # 记下真实的 RELEASE.xxx 标签
```

minio 的 compose 里现写的是 `latest`，这会让目标机 load 后各次可能不一致。**先把 `deploy/docker-compose.yml` 里
`image: minio/minio:latest` 改成上面看到的固定标签**，例如 `minio/minio:RELEASE.2024-08-17T01-24-54Z`。
下面用 `MINIO_IMG` 指代这个固定标签。

### 2.6 导出全部镜像为一个 tar

```bash
MINIO_IMG=minio/minio:RELEASE.2024-08-17T01-24-54Z   # 换成 2.5 实到的标签

cd /opt/build/offline-demo
docker save \
  mysql:8.4 \
  redis:8.2 \
  rabbitmq:4.3-management \
  "$MINIO_IMG" \
  offline-demo-backend:1.0 \
  offline-demo-frontend:1.0 \
  -o all-images.tar

ls -lh all-images.tar      # 几百 MB ~ 1GB 量级
```

> 注意：`docker save` 会把依赖的基础镜像层（temurin、nginx）一并打进 app 镜像，所以只需保存这 **6 个镜像**。

### 2.7 下载 Docker 静态二进制 + compose（目标机装 Docker 用）

```bash
mkdir -p /opt/build/offline-demo/kit/docker
cd /opt/build/offline-demo/kit/docker

# 1) Docker 引擎静态包（docker-ce 官方静态编译版，免 rpm/deb 依赖）
wget https://download.docker.com/linux/static/stable/x86_64/docker-24.0.7.tgz

# 2) compose 独立二进制（让目标机能用 `docker compose`）
wget https://github.com/docker/compose/releases/download/v2.29.7/docker-compose-linux-x86_64
```

> 版本号以实际存在的为准：`docker-24.0.7.tgz` 换成 static 目录里的最新稳定版；compose 换成 GitHub releases 最新 v2.x。
> 若 GitHub/域名在某环境不好下，可在能翻墙处的机器下好了再转进去（构建机有网即可）。

### 2.8 组装离线包

```bash
cd /opt/build/offline-demo
mkdir -p kit/deploy/images
cp all-images.tar kit/deploy/images/all-images.tar
cp deploy/docker-compose.yml deploy/.env.example kit/deploy/
cp -r deploy/scripts kit/deploy/scripts
```

最终 `kit/` 结构：

```
offline-kit/
├── docker/                     # 目标机装系统用的，装完可删
│   ├── docker-24.0.7.tgz
│   └── docker-compose-linux-x86_64
└── deploy/                     # === 目标机 /opt/offline-demo 的内容
    ├── docker-compose.yml
    ├── .env.example
    ├── scripts/init-dirs.sh
    └── images/all-images.tar
```

打包（可选压缩，缩小 U 盘体积）：

```bash
cd /opt/build/offline-demo
tar -czf offline-kit.$(uname -m).tar.gz kit/
```

拷进 U 盘 / 内网摆渡机，交付给目标机。

---

## 3. 目标机操作（完全离线）

### 3.1 传输 + 预检

把 `offline-kit/` 整个拷到目标机（U 盘挂载后 cp，或内网 scp）：

```bash
uname -m                          # ★ 必须 x86_64
uname -r
modprobe overlay  2>/dev/null; modprobe br_netfilter 2>/dev/null
getenforce 2>/dev/null            # 若输出 Enforcing → 见下方 SELinux 处理
```

SELinux（仅当 `getenforce` 输出存在且为 Enforcing 时）：

```bash
sudo setenforce 0
# 持久化：编辑 /etc/selinux/config → SELINUX=disabled（没有该文件则说明系统无 SELinux，跳过）
```

### 3.2 装 Docker（静态二进制）

```bash
cd <offline-kit 所在目录>
tar -xzf docker/docker-24.0.7.tgz
sudo cp docker/* /usr/bin/        # 含 dockerd/docker/containerd/runc/ctr 等

sudo dockerd >/tmp/dockerd.log 2>&1 &    # 先手动起一次，验证 OK
docker version                    # 能打印 Client + Server 即成功
```

### 3.3 装 compose 插件（让 `docker compose` 空格命令可用）

```bash
sudo mkdir -p /usr/local/lib/docker/cli-plugins
sudo cp docker/docker-compose-linux-x86_64 /usr/local/lib/docker/cli-plugins/docker-compose
sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
docker compose version            # 能打印 Docker Compose version v2.x 即成功
```

### 3.4 用 systemd 托管 dockerd（开机自启，配合 `restart: unless-stopped`）

> 手动 `dockerd &` 重启就没了，正式环境必须接 systemd。先把 3.2 手动起的进程停掉（`sudo pkill dockerd`），再：

```bash
sudo tee /etc/systemd/system/docker.service >/dev/null <<'EOF'
[Unit]
Description=Docker Application Container Engine
After=network-online.target firewalld.service
Wants=network-online.target

[Service]
Type=notify
ExecStart=/usr/bin/dockerd
ExecReload=/bin/kill -s HUP $MAINPID
TimeoutSec=0
RestartSec=2
Restart=always
LimitNOFILE=infinity
LimitNPROC=infinity
LimitCORE=infinity
TasksMax=infinity
Delegate=yes
KillMode=process
OOMScoreAdjust=-500

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now docker
sudo systemctl status docker --no-pager
docker info 2>/dev/null | grep -E 'Storage Driver|Cgroup|Server Version'
```

### 3.5 放部署目录 + 导入镜像

```bash
sudo mkdir -p /opt/offline-demo
sudo cp -r <offline-kit>/deploy/* /opt/offline-demo/
cd /opt/offline-demo

docker load -i images/all-images.tar
docker images                      # 应看到 6 个镜像（mysql/redis/rabbitmq/minio/backend/frontend）
```

### 3.6 初始化目录权限（首次必做）

```bash
cd /opt/offline-demo
bash scripts/init-dirs.sh
```

> 这一步创建 `middleware/mysql/data` 等目录，并把 data 目录 chown 给 uid 999（容器内用户）。
> **不做这步，MySQL/Redis/RabbitMQ 会报 `mkdir ... Permission denied` 启动失败。**

### 3.7 改 `.env`（按需，可选）

```bash
cd /opt/offline-demo
cp .env.example .env       # 首次：从模板生成 .env
sudo vim .env              # 改密码等
```

- 改密码：`MYSQL_ROOT_PASSWORD` / `REDIS_PASSWORD` / `RABBITMQ_PASS` / `MINIO_ROOT_PASSWORD`（minio 密码 ≥8 位）
- 改完密码，backend 的连接信息在 compose 里用 `${...}` 引用，会自动同步，无需单独改
- 若默认端口（80/3306/6379/5672/9000/9001）与系统冲突，改 compose 里 `ports` 左侧的宿主机端口即可

### 3.8 启动整站

```bash
cd /opt/offline-demo
docker compose up -d
docker compose ps                  # 6 个容器应全部 Up
```

首次冷启动 MySQL 要初始化几秒~几十秒，`docker compose ps` 全 Up 后再做验收。

---

## 4. 验收

```bash
# 后端接口直连（四类中间件全 ok:true）
curl -s http://<目标机IP>:8080/api/health/all

# 前端首页（返回 HTML）
curl -I http://<目标机IP>/
```

浏览器打开 `http://<目标机IP>/`，点「🚀 测试全部」，**四张卡片全绿「正常」**即完整通过。

中间件管理台单独确认：

| 组件 | 验证方式 |
|---|---|
| MySQL | `docker exec mysql mysql -uroot -pmrlu -e "SELECT VERSION();"` |
| Redis | `docker exec redis redis-cli -a mrlu ping` → `PONG` |
| RabbitMQ | 浏览器 `http://<IP>:15672`（admin / mrlu） |
| MinIO | 浏览器 `http://<IP>:9001`（mrlu / mrlumrlu） |

**首装验收单**：

- [ ] 前端页面打开，`/api` 反代可达后端，四卡片全绿
- [ ] `curl :8080/api/health/all` 四服务 `ok:true`
- [ ] 重启目标机后 `docker compose ps` 自动恢复（systemd + `restart: unless-stopped`）
- [ ] 数据落在 `/opt/offline-demo/middleware/**`（mysql/redis/rabbitmq/minio 目录已生成文件）
- [ ] 时间正确（compose 已设 `TZ: Asia/Shanghai`；全站可加，按需补到每个 service）

---

## 5. 日常运维

```bash
cd /opt/offline-demo
docker compose ps                   # 状态
docker compose logs -f backend      # 跟后端日志
docker compose logs -f frontend     # 跟 nginx 日志
docker compose restart backend      # 单容器重启
docker compose down                 # 停全部（middleware 数据保留）
docker compose down -v              # ⚠️ 删网络等；bind mount 的 middleware 数据仍保留
docker compose up -d                # 再启动
```

**备份 / 迁移** = 直接拷 `/opt/offline-demo`（数据都在 `middleware/**` 固定路径）：

```bash
sudo tar -czf /opt/backup-$(date +%F).tar.gz -C /opt offline-demo
```

**升级应用版本（backend/frontend）**：在构建机重新 build + save 新的 app 镜像 tar，拷到目标机
`docker load -i` 后 `cp` 新 compose（若映射/端口变了）→ `docker compose up -d`。中间件不必动。

---

## 6. 常见问题排查

| 现象 | 原因 / 处理 |
|---|---|
| `Permission denied ... mkdir` | 没跑 `scripts/init-dirs.sh`，data 目录属主不是 uid 999，补跑一次 |
| `exec format error` | 镜像架构与机器不符（本场景 x86_64 不会遇到；若出现说明镜像打成了 arm64），重打 amd64 |
| `docker compose` 报 command not found | compose 插件没放到 `/usr/local/lib/docker/cli-plugins/`，回看 3.3 |
| dockerd 起不来 / storage driver 报错 | 内核不支持 overlay2：`modprobe overlay`；仍不行就换 `vfs`（`dockerd --storage-driver=vfs`，性能差但能跑） |
| 前端能开、点测试全红 | backend 没起来或连不上中间件：`docker compose logs backend` |
| 端口被占 | `ss -lntp` 排查，改 compose 里 `ports` 左侧宿主机端口 |
| MinIO 起不来 | `MINIO_ROOT_PASSWORD` 必须 ≥8 位（默认 `mrlumrlu`） |
| RabbitMQ 重启后丢数据 | 已设 `hostname: rabbitmq`（固定 mnesia 节点名），勿删 |
| 时间差 8 小时 | 所有中间件补 `TZ: Asia/Shanghai` 环境变量 |

---

## 7. 与本方案配套的仓库文件关系

| 文件 | 角色 |
|---|---|
| `deploy/docker-compose.yml` | 唯一编排文件（6 服务），目标机直接用 |
| `deploy/.env` | 集中管理密码 + APP_VERSION，`${VAR:-默认}` 引用 |
| `deploy/scripts/init-dirs.sh` | 首次初始化目录 + chown |
| `backend/Dockerfile` `frontend/Dockerfile` `frontend/nginx.conf` | 构建机打镜像用 |
| `frontend/.dockerignore` | **本次新增**：排除 node_modules/dist，保证构建干净 |

---

## 8. 架构不是 x86_64 怎么办（aarch64 快速对照）

- 2.2~2.3：jar 通用；镜像 `docker buildx build --platform linux/arm64` 或者在 arm64 构建机直接 build
- 2.7：Docker 静态包路径 `.../static/stable/aarch64/`，compose 用 `docker-compose-linux-aarch64`
- 2.6：中间件镜像用 `--platform linux/arm64` 拉取（mysql/redis/rabbitmq/nginx/temurin 均官方提供 arm64）
- 其余流程完全一致。**穿插混用 amd64/arm64 必报 `exec format error`，务必整套同架构。**

---

## 附录 A · 中间件位置对照表（部署后速查）

> 宿主机绝对路径 = `/opt/offline-demo/` + 表中「数据目录」。账号密码默认值见 `deploy/.env.example`，复制为 `.env` 改后以 `.env` 为准。

| 中间件 | 容器名 | 对外端口 | 管理台 | 数据目录（相对部署根） | 默认账号 / 密码 |
|---|---|---|---|---|---|
| MySQL | mysql | 3306 | — | `middleware/mysql/{data,conf,init}` | root / mrlu |
| Redis | redis | 6379 | — | `middleware/redis/data` | — / mrlu |
| RabbitMQ | rabbitmq | 5672 | 15672 | `middleware/rabbitmq/data` | admin / mrlu |
| MinIO | minio | 9000 | 9001 | `middleware/minio/data` | mrlu / mrlumrlu |
| backend | backend | 8080 | — | 无状态（不落盘） | — |
| frontend | frontend | 80 | — | 静态文件在镜像内 | — |

## 附录 B · 可选加固：用 healthcheck 让后端等中间件就绪再起

当前 `depends_on` 只保证「容器先起」，不保证「服务就绪」。要彻底自动化，给中间件加 healthcheck，后端等待健康后再起：

```yaml
  mysql:
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-pmrlu"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  redis:
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "mrlu", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    depends_on:
      mysql:      { condition: service_healthy }
      redis:      { condition: service_healthy }
      rabbitmq:   { condition: service_healthy }
      minio:      { condition: service_healthy }
```

> 这是「自动更新脚本化」阶段的标准姿势。首次部署先用手动流程跑通（§3），稳定后再加，避免过早复杂化。