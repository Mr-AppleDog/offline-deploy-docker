# Kunlun 离线交付平台 · 用户手册

> **Kunlun Package Studio** —— 在受控构建机上将代码仓库打包成可校验、可审计的离线交付包，供无外网的 Kylin V10 `amd64` / `arm64`（飞腾 / 鲲鹏）服务器使用。

本手册面向平台使用人员（交付 / 运维工程师），讲解平台从登录、配置到出包、再到目标机部署的完整流程。平台的部署总则与目标机手工操作另见 [`部署手册.md`](部署手册.md) 与 [`docs/`](docs/)。

---

## 目录

1. [平台能做什么](#1-平台能做什么)
2. [运行环境与安全约束](#2-运行环境与安全约束)
3. [启动平台](#3-启动平台)
4. [首次登录（管理令牌）](#4-首次登录管理令牌)
5. [功能模块一览](#5-功能模块一览)
6. [模块详解：总览](#61-总览-dashboard)
7. [模块详解：项目](#62-项目-projects)
8. [模块详解：部署配置](#63-部署配置-profiles)
9. [模块详解：离线制品库与数据库脚本库](#64-离线制品库与数据库脚本库-artifacts-sqlscripts)
10. [模块详解：构建与交付](#65-构建与交付-builds)
11. [端到端工作流（完整示例）](#11-端到端工作流完整示例)
12. [交付物说明](#12-交付物说明)
13. [在目标机上使用交付包](#13-在目标机上使用交付包)
14. [平台运维与持久化](#14-平台运维与持久化)
15. [凭据与密钥管理](#15-凭据与密钥管理)
16. [常见问题与排错](#16-常见问题与排错)

---

## 1. 平台能做什么

平台解决一个核心问题：**把“构建机上已备好的镜像 tar 与数据库 SQL”变成“断网的目标服务器能直接装的离线包”**，并保证全过程可校验、可审计。前后端应用镜像由用户在平台外构建并 `docker save` 成 tar，数据库 SQL 直接提交，平台只负责编排、校验与打包，不再从源码构建、不再拉取代码仓库。

主要能力：

- **项目配置**：登记应用标识（appKey）、当前版本与健康路径，用于生成镜像名（`<appKey>-backend:<版本>`）与 Compose 门禁；**无需配置代码仓库、无需仓库分析**。
- **页面上传驱动**：前后端镜像 tar 与数据库 SQL 均通过页面上传入库，平台自动计算 SHA256；构建时从库中下拉选择，可复用、可溯源。
- **数据库脚本库**：初始化 SQL（随 bootstrap 包入 `database/init`）与迁移 SQL（入 `database/migrations/<版本>`）分类入库，构建时按目标版本选择入包。
- **中间件注册表**：内置 14 类中间件（MySQL、PostgreSQL、人大金仓 KingbaseES、达梦 DM8、瀚高 HighGo、MongoDB、Redis、RabbitMQ、Kafka、RocketMQ、Elasticsearch、MinIO、Nginx、东方通 TongWeb），新增中间件只需加一条目录定义。
- **双架构目标**：Kylin V10 `amd64` 与 `arm64`，产物命名贯穿镜像 tar、Compose 与安装脚本校验。
- **按站点独立凭据**：每个中间件独立账号密码，页面提供一键强密码生成，AES-GCM 加密持久化，API 与页面均不回显明文。
- **离线制品管理**：上传 Docker Engine、Compose 及各中间件镜像 tar（按架构分区），自动计算 SHA256。
- **两类交付包**：
  - **完整初始化包**（bootstrap）—— 用于全新服务器，包含 Docker、中间件、应用、脚本、文档全套。
  - **应用更新包**（app-update）—— 用于已有环境，只含新版本应用镜像、Compose 与可选数据库迁移。
- **异步串行构建**：实时任务阶段、日志、镜像清单与 SHA256，失败保留现场。
- **平台自身可入库**：元数据落真实 MySQL、构建产物落 MinIO，未配置时自动回退本地 JSON 与本地文件。

---

## 2. 运行环境与安全约束

### 2.1 构建机要求

平台必须部署在一台**受控构建机**上，该机器需要：

| 能力 | 用途 | 后端检测 |
|---|---|---|
| JDK 17 + Maven | 运行后端 | — |
| tar / gzip | 打包交付物、校验 | `tar --version` |
| Docker Engine | 镜像 `load` / `inspect` / compose 校验 | `docker info` |

> 上传驱动流程下，构建不再拉取代码仓库、也不再从源码构建镜像，因此不再依赖 Git 与 Docker Buildx（首页仍展示其检测状态，但非构建必需）。**tar 与 Docker Engine 任一项不可用都会导致构建失败。**

### 2.2 安全约束（务必遵守）

- **Docker Socket 挂载 = 主机级权限**：平台后端为执行镜像 `load` / `inspect` 与 `compose config` 校验会挂载 `/var/run/docker.sock`，等同于在构建机上拥有 root 构建权限。**平台只能部署在受控构建机，严禁直接暴露到公网。**
- **管理令牌**：必须设置足够长的 `KUNLUN_ADMIN_TOKEN`；未设置则 `/api/platform/*` 不做鉴权（仅限隔离调试）。
- **敏感文件**：生成的两份 Compose（`compose.middleware.yml` / `compose.app.yml`）内嵌明文凭据，属于站点专用敏感材料，权限必须 `root:root 0600`，不得进入公共仓库或跨站点复用。

---

## 3. 启动平台

### 方式一：本地开发启动

后端要求 JDK 17、Maven、tar、Docker Engine；前端要求 Node.js 20+。**项目不使用也不兼容 Anaconda。**

```powershell
# 1. 设置管理令牌（越长越好）
$env:KUNLUN_ADMIN_TOKEN='请设置足够长的管理令牌'

# 2. 启动后端
cd backend
mvn spring-boot:run
```

```powershell
# 3. 启动前端
cd frontend
npm ci
npm run dev
```

访问 `http://localhost:5173`。平台数据默认保存在项目根目录的 `.kunlun-builder/`。

### 方式二：容器启动（推荐用于常驻构建机）

```powershell
$env:KUNLUN_ADMIN_TOKEN='请设置足够长的管理令牌'
docker compose -f compose.platform.yml up -d --build
```

访问 `http://localhost:8088`。

> 容器模式下，后端会挂载 Docker Socket。若要让平台能访问代码仓库或额外离线介质目录，需将这些目录挂载到后端容器后，在页面里填写**容器内路径**。

### 关键环境变量速查

| 变量 | 是否必需 | 含义 |
|---|---|---|
| `KUNLUN_ADMIN_TOKEN` | **必需** | 平台管理令牌，用于所有 `/api/platform/*` 鉴权 |
| `KUNLUN_SECRET_KEY` | 可选 | 凭据加密主密钥（Base64 编码的 32 字节）；未设则首次在数据目录生成 `master.key` |
| `KUNLUN_METADB_URL` | 可选 | 平台元数据库 JDBC 地址，设了落 MySQL，否则回退本地 JSON |
| `KUNLUN_METADB_USER` / `KUNLUN_METADB_PASSWORD` | 可选 | 元数据库账号密码 |
| `KUNLUN_STORAGE_TYPE` | 可选 | 产物存储后端，`minio` 则落对象存储，缺省 `local` |
| `KUNLUN_MINIO_ENDPOINT` / `_ACCESS_KEY` / `_SECRET_KEY` / `_BUCKET` | 可选 | MinIO 连接信息 |

> 第 14 节给出完整的“对接真实 MySQL + MinIO”配置示例。

---

## 4. 首次登录（管理令牌）

平台所有写操作和 `/api/platform/*` 接口都受管理令牌保护。

1. 浏览器打开平台地址（本地开发 `http://localhost:5173`，容器 `http://localhost:8088`）。
2. 首次访问若返回 `401`，页面会弹出输入框，要求输入 `KUNLUN_ADMIN_TOKEN` 的值。
3. 令牌保存到浏览器 `localStorage`（键 `kunlun-admin-token`），后续请求自动带 `Authorization: Bearer <token>`。
4. 令牌失效或想切换账号：在浏览器控制台执行 `localStorage.removeItem('kunlun-admin-token')` 后刷新页面，会再次弹出输入框。

> 令牌校验使用常数时间比较，可抵抗时序攻击；但请务必设置长且随机的令牌值。

---

## 5. 功能模块一览

左侧导航栏六个模块，对应交付流程的六个阶段：

| 模块 | 图标 | 解决的问题 |
|---|---|---|
| **总览** | ⌂ | 一屏掌握项目数、配置数、制品数、脚本数、构建数；构建机能力自检 |
| **项目** | ⌘ | 登记应用标识、版本与健康路径，用于镜像命名与 Compose 门禁 |
| **部署配置** | ◇ | 配置“装到哪个站点”，每站点独立中间件凭据 |
| **离线制品库** | ▣ | 上传并管理“可信 tar 介质”，按架构分区并校验 SHA256 |
| **数据库脚本库** | ▤ | 上传初始化 SQL 与迁移 SQL，构建时按版本选择入包 |
| **构建与交付** | ▷ | 把前几者组合成可下载、可校验的离线包 |

推荐顺序：**项目 → 部署配置 → 离线制品库 → 数据库脚本库 → 构建与交付**。

---

## 6.1 总览（Dashboard）

总览页一屏展示全局状态：

- **顶部指标卡片**：已接入项目数、部署配置数、离线制品数、数据库脚本数、构建任务数（含执行中数量）。
- **构建环境卡片**：列出 Git、tar/gzip、Docker Engine、Docker Buildx 四项是否可用（绿点 / 红点）。**其中 tar/gzip 与 Docker Engine 为构建必需，必须绿；Git 与 Buildx 在上传驱动流程下不再参与构建，仅作展示。**
- **最近构建卡片**：最新 4 条任务的包类型（B=初始化包 / U=更新包）、版本、阶段与状态。
- **新建构建任务**按钮：直接跳到构建表单。
- 顶部状态栏 `构建机就绪 / Docker 未就绪` 实时反映 Docker 是否可用；右上角 `↻` 手动刷新全部数据。

---

## 6.2 项目（Projects）

本模块登记“要交付的应用标识与健康路径”。上传驱动流程下**不再需要配置代码仓库，也不再需要仓库分析**。

### 6.2.1 创建 / 编辑项目

点击 **＋ 新建项目**，填写：

| 字段 | 说明 | 示例 |
|---|---|---|
| 项目名称 | 展示用 | `Kunlun 应用` |
| 应用标识（appKey） | **用于生成镜像名**，如 `<appKey>-backend:<版本>` | `kunlun-app` |
| 当前版本 | 语义化版本，作为新建构建任务时的默认目标版本 | `1.1.1` |
| 后端健康路径 | 写入应用 Compose 的健康门禁 | `/api/health/live` |
| 前端健康路径 | 同上 | `/` |
| 描述 | 任意 | 项目说明 |

> 应用标识一旦用于构建即会进入镜像名和交付物路径，**不要随意改名**。

### 6.2.2 项目详情

项目详情页展示应用标识、当前版本与前后端健康路径。前后端镜像 tar 与数据库 SQL 不在此页上传——分别见 §6.4 离线制品库与数据库脚本库。中间件依赖在“部署配置”页手动勾选，不再依赖仓库分析。

---

## 6.3 部署配置（Profiles）

本模块管理“装到目标站点时的站点参数与中间件凭据”。**每个站点一份独立配置。**

### 6.3.1 创建 / 编辑部署配置

点击 **＋ 新建配置**，填写：

| 字段 | 说明 | 默认 |
|---|---|---|
| 配置名称 | 站点名 | — |
| 环境 | 如 `生产环境` | `生产环境` |
| 目标架构 | `麒麟 V10 amd64` / `麒麟 V10 arm64` | amd64 |
| 前端端口 | 目标机前端暴露端口 | 80 |
| 时区 | 容器时区 | `Asia/Shanghai` |
| Java 参数 | 后端 JVM 参数 | `-Xms256m -Xmx1024m` |
| 中间件 | 按分类勾选所需组件 | 新建时默认勾选 mysql/redis/rabbitmq/minio |

中间件按分类（数据库 / 缓存 / 消息队列 / 对象存储 / Web 等）以标签形式列出，**按需勾选**。勾选后下方展开该中间件的凭据字段（账号、密码、库名等，由注册表定义）。

### 6.3.2 一键生成强密码

点击 **⚄ 一键生成全部强密码**，平台为当前勾选中间件的所有“密码类”字段各生成一组独立强密码并填入表单。

> 密码字段在编辑模式下留空表示**保持原密文不变**；填入新值则覆盖。

### 6.3.3 加密保存与凭据安全

- 保存后凭据以 **AES-GCM** 加密持久化，页面与 API 均不回显明文（列表只显示“凭据已配置 / 待配置”）。
- 加密主密钥来自 `KUNLUN_SECRET_KEY`（或数据目录的 `master.key`），**不落库**。
- 每次保存部署配置会产生新的**修订号（r1、r2 …）**，构建任务会绑定创建时的修订号；若配置在任务创建后被修改，构建会拒绝执行并提示“部署配置在任务创建后已修改，请重新创建构建任务”。

### 6.3.4 凭据轮换边界（务必理解）

> **应用更新包只能复用目标站点已有凭据。** 修改本页密码会产生新的配置修订，属于**凭据轮换**，不能当作普通应用更新使用。修改运行中中间件密码必须通过独立运维流程同步修改已运行实例。

---

## 6.4 离线制品库与数据库脚本库（Artifacts & SqlScripts）

本模块管理两类入库制品：**镜像 tar 介质**（按组件 + 版本 + 架构索引）与**数据库 SQL 脚本**（按类型 + 目标版本索引）。两者均通过页面上传按钮入库，平台自动计算 SHA256，构建时下拉选择、可复用。

### 6.4.1 上传镜像制品

点击 **＋ 导入制品**，在弹窗中：

| 字段 | 说明 |
|---|---|
| 组件 | 下拉：`Docker Engine` / `Docker Compose` / 后端应用镜像 / 前端应用镜像 + 14 类中间件 |
| 架构 | `amd64 (x86_64)` / `arm64 (aarch64)` |
| 版本 | 如 `29.7.0`（Docker）/ `8.4`（MySQL）；**应用镜像版本必须等于构建目标版本** |
| 制品文件 | 浏览器选择本地 tar/tgz 上传 |

- 应用镜像须在平台外 `docker tag <appKey>-backend:<版本>` 再 `docker save` 成 tar 后上传；镜像名规范为 `<appKey>-<role>:<版本>`。
- 上传后平台计算 **SHA256**，记录大小与上传时间；列表展示组件、版本、架构、文件名、大小、SHA256、上传时间。

### 6.4.2 上传数据库脚本

进入“数据库脚本库”页，点击 **＋ 上传脚本**：

| 字段 | 说明 |
|---|---|
| 类型 | `初始化 (INIT)`（随 bootstrap 包入 `database/init`）/ `迁移 (MIGRATION)`（入 `database/migrations/<目标版本>`） |
| 名称 | 脚本展示名，如 `schema-init` |
| 目标版本 | 该脚本适用的应用版本，如 `1.1.1`；迁移脚本必须与构建目标版本一致 |
| SQL 文件 | 浏览器选择本地 `.sql` 上传 |

- 初始化 SQL 在目标机随 MySQL 首次启动执行（按文件名排序）；迁移 SQL 由 `install-app-update.sh` 在升级时执行。
- 多个脚本可分多次上传；构建表单中可多选入包。**当前 init SQL 仅对 MySQL 生效**，使用其它数据库时需扩展安装脚本逻辑。

### 6.4.3 制品与构建的关系

- **完整初始化包**必须为 `Docker Engine`、`Docker Compose` 以及部署配置中声明的**每一个中间件**各选一个制品，且**架构必须与目标架构一致**，否则创建任务时报错；初始化 SQL 可选（不选则不带 init SQL）。
- **应用更新包**不依赖中间件制品（复用站点已有的），但须选对应更新范围的前后端应用镜像；迁移 SQL 在勾选“需要数据库迁移”时必选。

> 建议：为每个目标架构（amd64 / arm64）提前上传一套完整的 Docker + Compose + 常用中间件制品，避免临到构建才发现缺料。

---

## 6.5 构建与交付（Builds）

本模块把项目、配置、制品组合成离线包。

### 6.5.1 新建构建任务

点击 **＋ 新建构建**，填写：

| 字段 | 适用 | 说明 |
|---|---|---|
| 项目 | 两者 | 选择项目（无需预先分析） |
| 部署配置 | 两者 | 选择站点配置（显示修订号） |
| 包类型 | 两者 | `完整初始化包` / `应用更新包` |
| 目标架构 | 两者 | amd64 / arm64 |
| 起始版本 | 更新包 | 当前已部署版本 |
| 目标版本 | 两者 | 本次交付版本，须为语义化 `x.y.z` |
| 包修订号 | 初始化包 | `r1`、`r2`…，同版本不同批次递增 |
| 离线制品版本 | 初始化包 | 为 Docker、Compose + 部署配置声明的每个中间件各选一个制品（应用镜像可选） |
| 数据库初始化 SQL | 初始化包 | 从脚本库多选 INIT 脚本，入 `database/init`（可选） |
| 应用镜像 | 更新包 | 按更新范围选前后端应用镜像（版本须等于目标版本） |
| 更新范围 | 更新包 | 勾选 后端 / 前端 |
| 需要数据库迁移 | 两者 | 勾选后从脚本库多选 MIGRATION 脚本（目标版本须一致） |

**校验规则**（创建时即校验，不通过直接报错）：

- 无需仓库分析；项目仅需登记 appKey 与健康路径。
- 目标版本须为 `x.y.z`（可带 `-suffix`）；更新包的起始版本与目标版本不能相同。
- 初始化包：制品集合须包含 Docker + Compose + 部署配置声明的每个中间件，同一组件不能选多个版本，架构须匹配；应用镜像可选。
- 应用镜像版本必须等于目标版本（镜像名 `<appKey>-<role>:<目标版本>`）。
- 选择的 SQL 脚本须存在：INIT 脚本类型须为初始化；MIGRATION 脚本类型须为迁移且目标版本等于构建目标版本。
- 声明数据库迁移时必须至少选一个迁移 SQL 脚本。
- 包修订号格式须为 `r1`、`r2` …。
- 同 `appKey + 目标版本` 的交付物若已存在，会报错提示“提高包修订号或目标版本”。

### 6.5.2 任务执行与监控

任务创建后立即进入队列，**串行执行**（同一时刻只跑一个）。执行阶段与进度：

```
构建环境预检查(3%) → 校验构建素材(10%) → 加载应用镜像 tar(24%)
 → [初始化包]校验中间件镜像(48%) → 组装离线包(62%)
 → 生成校验文件并压缩(82%) → 交付物自检完成(100%)
```

- 状态：`排队中` → `构建中` → `成功` / `失败` / `已取消`。
- 选中任务后右侧显示**实时日志**（运行中每 2.5 秒自动轮询刷新）。
- 失败时日志与错误信息保留在现场，**平台不会执行 `docker prune`**，便于排查。
- 成功后任务行出现 **下载** 按钮，并展示该包的 **SHA256**（可一键复制）。

### 6.5.3 下载与校验

- 点 **下载** 获取 `.tar.gz`；页面同时显示 SHA256，**请与下载文件一并保存**。
- 也可调用 `GET /api/platform/builds/{id}/checksum` 单独取校验值。
- 目标机部署时会用 `sha256sum -c SHA256SUMS` 重新校验包内所有文件。

---

## 11. 端到端工作流（完整示例）

以“首次为某站点交付一套完整初始化包”为例：

1. **总览**：确认 tar/gzip 与 Docker Engine 可用（绿）。
2. **项目**：新建项目，应用标识 `kunlun-app`，当前版本 `1.1.1`，后端健康路径 `/api/health/live`，前端健康路径 `/`。无需配仓库、无需分析。
3. **部署配置**：新建配置 `站点A-生产`，目标架构 amd64，勾选所需中间件（mysql、redis、rabbitmq、minio 等），点 **一键生成全部强密码**，**加密保存**。
4. **离线制品库**：
   - 上传 `docker-engine`（amd64）、`docker-compose`（amd64）。
   - 上传上一步勾选的每个中间件镜像 tar（amd64）。
   - 在平台外 `docker tag kunlun-app-backend:1.1.1` 后 `docker save` 成 tar，以 `app-backend` 组件上传（frontend 同理）。
5. **数据库脚本库**：上传初始化 SQL（类型 INIT，目标版本 `1.1.1`）。
6. **构建与交付**：新建构建，选项目与配置，包类型 `完整初始化包`，目标版本 `1.1.1`，包修订号 `r1`；为 Docker/Compose/各中间件制品各选一个版本，多选初始化 SQL；提交构建，观察日志直至 **成功**，下载 `kunlun-bootstrap-1.1.1-r1-kylin-v10-amd64.tar.gz` 并保存 SHA256。

后续版本升级：在 **构建与交付** 选 `应用更新包`，填起始版本 `1.1.1`、目标版本 `1.1.2`，勾选更新范围并选对应应用镜像；先在 **数据库脚本库** 上传目标版本 `1.1.2` 的迁移 SQL（类型 MIGRATION），构建时勾选“需要数据库迁移”并多选该迁移脚本，提交即可得到 `kunlun-app-update-1.1.2-kylin-v10-amd64.tar.gz`。

---

## 12. 交付物说明

### 12.1 完整初始化包

文件名：`kunlun-bootstrap-<版本>[-rN]-kylin-v10-<arch>.tar.gz`

包内结构（构建时由 `BuildWorker.assemble` 生成）：

```
kunlun-bootstrap-<ver>-r<N>-kylin-v10-<arch>/
├── manifest.env              # 包类型、版本、目标平台、镜像名、中间件清单与版本
├── images.txt                # 镜像名|ID|平台|tar相对路径|SHA256
├── SHA256SUMS                 # 包内全部文件的 SHA256 汇总
├── build-spec.json            # 本次构建的不可变快照
├── docker/install/            # Docker Engine tgz、Compose 二进制、daemon.json、docker.service
├── middleware/
│   ├── compose.middleware.yml # 中间件 Compose（内嵌站点凭据）
│   ├── middleware.list        # 中间件组件清单
│   ├── middleware.spec.json   # 中间件规格（备份策略、健康检查等）
│   └── <组件>/image/*.tar     # 各中间件镜像 tar
├── application/
│   ├── compose.app.yml        # 应用 Compose（内嵌业务凭据副本）
│   └── images/<版本>/*.tar    # backend / frontend 镜像 tar
├── database/
│   ├── init/                 # 首次初始化 SQL
│   └── migrations/<版本>/     # 分版本迁移 SQL
├── scripts/                  # install-bootstrap.sh 等脚本（已按目标架构参数化）
├── README.md / 部署手册.md / docs/   # 文档
```

### 12.2 应用更新包

文件名：`kunlun-app-update-<目标版本>-kylin-v10-<arch>.tar.gz`

包内只含：

```
├── manifest.env              # 含 FROM_VERSION / TO_VERSION / UPDATE_SCOPE / DB_MIGRATION_REQUIRED
├── images.txt
├── SHA256SUMS
├── build-spec.json
├── application/
│   ├── compose.app.yml       # 新版本应用 Compose
│   └── images/<目标版本>/*.tar
├── database/migrations/<目标版本>/   # 可选迁移 SQL
└── scripts/
    ├── common.sh
    └── install-app-update.sh
```

> 更新包**不携带** Docker、中间件镜像、中间件 Compose 与中间件数据，不会修改中间件账号密码。

---

## 13. 在目标机上使用交付包

目标机要求：**Kylin V10，systemd，root 权限，具备 `sha256sum` / `tar` / `systemctl` / `ss` / `flock`**，且架构与包一致（amd64 包须在 `x86_64` 上、arm64 包须在 `aarch64` 上）。

### 13.1 完整初始化部署

1. 将 `kunlun-bootstrap-*.tar.gz` 传到目标机。
2. 校验外层 SHA256：

   ```bash
   sha256sum -c kunlun-bootstrap-*.tar.gz.sha256
   ```
3. 解压：

   ```bash
   tar -xzf kunlun-bootstrap-1.1.1-r1-kylin-v10-amd64.tar.gz
   cd kunlun-bootstrap-1.1.1-r1-kylin-v10-amd64
   ```
4. 执行安装脚本（root）：

   ```bash
   sudo bash scripts/install-bootstrap.sh
   ```

   脚本会：校验包内全部 SHA256（`sha256sum -c SHA256SUMS`）→ 校验包类型 / 版本 / 平台 → 安装 Docker 与 Compose（data-root 指向 `/opt/Kunlun/docker/data`）→ 导入全部镜像 → 安装两份 Compose → 初始化目录与权限 → 创建 `kunlun-net` → 启动中间件并验收健康 → 启动应用并验收健康。

5. 验收成功标准（见 [`部署手册.md`](部署手册.md) §7）：六个镜像标签/ID/平台与清单一致、Docker Root Dir 正确、四个中间件与 backend/frontend 均 `healthy`、`/api/health/all` 返回 `"ok":true`。

### 13.2 应用升级部署

1. 将 `kunlun-app-update-*.tar.gz` 传到目标机并校验外层 SHA256。
2. 解压后执行：

   ```bash
   sudo bash scripts/install-app-update.sh
   ```

   脚本会：校验包内 SHA256 → 校验当前中间件健康 → **备份数据库与当前 app Compose** → 导入新应用镜像 → 执行数据库迁移（如声明）→ 替换 `application/compose.app.yml` → 只重建 backend/frontend → 业务验收。

### 13.3 日常运维脚本

部署完成后，`/opt/Kunlun/scripts/` 下提供：

| 脚本 | 作用 |
|---|---|
| `start.sh` / `stop.sh` | 启停应用与中间件 |
| `status.sh` | 查看运行状态 |
| `check-middleware.sh` | 中间件健康检查 |
| `backup.sh` | 数据库与配置备份 |
| `restore.sh` | 恢复（建议先恢复到演练目录验证） |

> 首次部署成功后，**必须完成一次备份与恢复演练**才算交付完成。

---

## 14. 平台运维与持久化

### 14.1 默认本地存储

未配置任何存储变量时：

- 平台元数据（项目 / 配置 / 制品 / 构建记录）落在 `<数据目录>/platform-state.json`。
- 构建产物落在 `<数据目录>/deliveries/`。
- 加密主密钥落在 `<数据目录>/master.key`（首次自动生成）。

本地开发数据目录默认是项目根的 `.kunlun-builder/`；容器模式挂载到 `/data`。

### 14.2 对接真实 MySQL + MinIO

要“平台自身入库、产物入对象存储”，设置以下环境变量后重启平台：

```bash
# 平台元数据库
export KUNLUN_METADB_URL='jdbc:mysql://192.168.149.128:3306/kunlun_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai'
export KUNLUN_METADB_USER='root'
export KUNLUN_METADB_PASSWORD='mrlu'

# 产物对象存储
export KUNLUN_STORAGE_TYPE='minio'
export KUNLUN_MINIO_ENDPOINT='http://192.168.149.128:9000'
export KUNLUN_MINIO_ACCESS_KEY='mrlu'
export KUNLUN_MINIO_SECRET_KEY='mrlumrlu'
export KUNLUN_MINIO_BUCKET='kunlun-platform'
```

- 设了 `KUNLUN_METADB_URL` 后，平台自动 `CREATE TABLE IF NOT EXISTS kunlun_record` 并持久化；未设则回退本地 JSON。
- 设了 `KUNLUN_STORAGE_TYPE=minio` 后，制品与交付物 tar 上传 MinIO，下载走对象存储；未设则回退本地文件。
- `KUNLUN_SECRET_KEY`（或 `master.key`）**始终不落库**，是凭据加密密钥，务必随平台部署一并保留。

### 14.3 数据目录与产物路径

| 内容 | 本地模式路径 | 说明 |
|---|---|---|
| 元数据 JSON | `.kunlun-builder/platform-state.json` | 配置 MySQL 后入库 |
| 制品存储 | `.kunlun-builder/artifacts/` | 配置 MinIO 后入对象存储 |
| 交付物 | `.kunlun-builder/deliveries/<appKey>/<版本>/` | 同上 |
| 构建日志 | `.kunlun-builder/logs/<taskId>.log` | 构建详情页读取 |
| 构建工作目录 | `.kunlun-builder/workspaces/build/<taskId>/` | 构建成功后清理 sources |
| 主密钥 | `.kunlun-builder/master.key` | **切勿丢失** |

---

## 15. 凭据与密钥管理

### 15.1 三类敏感数据

1. **管理令牌**（`KUNLUN_ADMIN_TOKEN`）：平台访问鉴权，通过环境变量注入，不落库。
2. **加密主密钥**（`KUNLUN_SECRET_KEY` / `master.key`）：AES-GCM 加密站点凭据用，**不落库**。
3. **站点中间件凭据**：在“部署配置”页填入，加密后入库（或入 JSON），页面与 API 不回显明文。

### 15.2 密钥丢失的后果

> `master.key`（或 `KUNLUN_SECRET_KEY`）丢失后，**已加密的站点凭据将无法解密恢复**。请将主密钥纳入与平台部署同等强度的备份保管。

### 15.3 凭据轮换流程

1. 在 **部署配置** 编辑该站点配置，点 **一键生成全部强密码**（或手填新值）。
2. **加密保存**，配置修订号自增（如 r2 → r3）。
3. 重新创建构建任务（旧任务因修订号不匹配会被拒绝）。
4. 在目标机同步修改**已运行中间件**的对应凭据——**这一步必须在中间件层面独立完成，应用更新包不会代为修改中间件密码**。

### 15.4 Compose 文件保管

两份 Compose（`compose.middleware.yml`、`compose.app.yml`）内嵌明文凭据，是站点专用敏感文件：

- 目标机权限 `root:root 0600`。
- 含两份 Compose 的离线 tar 同样敏感，不得进入公共仓库或跨站点复用。

---

## 16. 常见问题与排错

| 现象 | 原因 / 处理 |
|---|---|
| 首页“Docker 未就绪” | 构建机未安装 Docker Engine，或后端容器未挂载 `/var/run/docker.sock` |
| `401 需要有效的管理令牌` | 未设置 `KUNLUN_ADMIN_TOKEN`，或浏览器 `localStorage` 中的令牌与后端不一致；清除 `kunlun-admin-token` 后重新输入 |
| 上传制品 / SQL 失败 | 文件过大、格式不符（须为 tar/tgz 或 .sql），或网络中断；multipart 上限 20GB，确认文件与架构匹配后重试 |
| 创建任务报“初始化包必须选择 …” | 制品集合与部署配置声明的中间件不一致，或缺 Docker/Compose，或选了同组件多版本 |
| 创建任务报“制品架构 … 与目标 … 不一致” | 选了与目标架构不匹配的制品；回到制品库上传对应架构版本 |
| 创建任务报“迁移脚本 … 目标版本 … 不一致” | 选了目标版本不匹配的迁移 SQL；上传或选择与构建目标版本一致的脚本 |
| 创建任务报“声明数据库迁移时必须选择迁移 SQL 脚本” | 勾了“需要数据库迁移”却未选迁移脚本；去数据库脚本库上传并多选 |
| 创建任务报“同名交付物已存在” | 同 `appKey + 目标版本` 已出过包；提高包修订号或目标版本 |
| 构建失败“部署配置在任务创建后已修改” | 配置被改过，修订号变了；重新创建构建任务绑定最新修订号 |
| 构建失败“缺少应用镜像制品 app-backend/app-frontend” | 未在制品库上传前后端镜像 tar；在平台外 `docker tag <appKey>-backend:<版本>` 再 `docker save` 后上传 |
| 构建失败“镜像架构不是 linux/amd64” | 应用/中间件制品架构与目标不匹配；上传对应架构的 tar |
| 下载失败“该任务还没有可下载的交付物” | 任务尚未成功；等待构建完成 |
| 目标机 `install-bootstrap.sh` 报“目标机架构必须为 x86_64” | 在 arm64 机器上跑了 amd64 包（或反之）；使用与机器架构匹配的包 |
| 目标机报“离线包缺少文件” | 包被截断或被改动；重新下载并校验外层 `.sha256` |

### 16.1 构建失败后的现场保留

平台在构建失败时**不会清理工作目录、不会执行 `docker prune`**。可在 `.kunlun-builder/workspaces/build/<taskId>/` 查看组装现场与导出镜像，结合 `.kunlun-builder/logs/<taskId>.log` 定位根因。修复后重新提交任务即可（注意同名交付物已存在时需提高修订号）。

### 16.2 日志与审计

- 每个构建任务有独立日志，可在“构建与交付”详情页查看，或读取 `.kunlun-builder/logs/<taskId>.log`。
- `manifest.env`、`images.txt`、`build-spec.json`、`SHA256SUMS` 构成完整审计链：可追溯包类型、版本、镜像 ID 与 SHA256（上传驱动流程下不再含源码 Commit，制品与 SQL 均以上传入库的 SHA256 为准）。

---

## 附：相关文档

- [`部署手册.md`](部署手册.md) —— 离线部署总则、固定决策、成功标准
- [`docs/目录规划.md`](docs/目录规划.md) —— `/opt/Kunlun` 目录与权限
- [`docs/V1-手工版.md`](docs/V1-手工版.md) —— 全新服务器手工部署
- [`docs/应用升级手工版.md`](docs/应用升级手工版.md) —— 应用与数据库手工升级
- [`docs/CI-CD.md`](docs/CI-CD.md) —— 平台自身的 GitOps 流水线
