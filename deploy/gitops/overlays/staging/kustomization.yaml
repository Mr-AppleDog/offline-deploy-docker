apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 所有资源落到独立 namespace,与 default / docloom 隔离
namespace: offline-delivery

resources:
  - ../../base

# 镜像坐标:base 里写的是逻辑名 offline-delivery-backend:placeholder / offline-delivery-frontend:placeholder,
# 这里改写成本地 registry 的真实坐标 + 占位 tag。
# 实际 tag 由 CI workflow 更新并 force-push 到 deploy/staging 分支(Argo 据此同步)。
images:
  - name: offline-delivery-backend
    newName: localhost:5000/offline-delivery-backend
    newTag: placeholder
  - name: offline-delivery-frontend
    newName: localhost:5000/offline-delivery-frontend
    newTag: placeholder
